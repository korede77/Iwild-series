{% extends 'base.html.twig' %}

{% block title %}Add category{% endblock %}

{% block body %}
    <h1>Add category : </h1>

    <div>
        {{ form_start(form) }}
            {{ form_row(form.name) }}
            <button class="btn btn-dark">{{ button_label|default('Save') }}</button>
        {{ form_end(form) }}
    </div>
    <br>
    <div>
        <a href="{{ path('app_index') }}">
            <button type="button" class="btn btn-danger">Retour à l'accueil</button>
        </a>
    </div>



{% endblock %}
