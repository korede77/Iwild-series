{% extends 'base.html.twig' %}

{% block body %}
    <h2> {{ program.title }}</h2>
    <div class=" container align-content-center">
        <div class="card text-white bg-dark mb-3" style="max-width: 70rem;">
            <div class="card-header"><h3>Episode {{ episode.id }} de {{ program.title }}</h3></div>
            <div class="card-body">
                <h5 class="card-title"> Titre:  {{episode.title}}</h5>
                <p class="card-text">Episode  {{ episode.number }}.</p>
                <p class="card-text">Synopsis: {{ episode.synopsis }}.</p>
            </div>
        </div>
    </div>
    <div class=" container align-content-center">
        <div class="card text-white bg-dark mb-3" style="max-width: 70rem;">
            <div class="card-header">
                <h3> <a href="/wild/season/{{ season.id }}"> Saison{{ season.id }} </a> </h3>
            </div>
            <div class="card-body">
                <h5 class="card-title">Sortie: {{ season.year }}</h5>
                <p class="card-text">{{ season.description }}.</p>
            </div>
        </div>
    </div>
    <div class=" container align-content-center">
        <div class="card text-white bg-dark mb-3" style="max-width: 70rem;">
            <div class="card-header">
                <h3> Série - <a href="{{ path('wild_show', {'slug': program.title|replace({' ': '-'})|lower }) }}"  >
                         {{ program.title }}
                    </a>
                </h3>
            </div>
        </div>
    </div>

    <a href="{{ path('wild_index') }}">
        <button type="button" class="btn btn-danger">  Retour</button>
    </a>
{% endblock %}
