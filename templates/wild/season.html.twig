{# templates/wild/program.html.twig #}
{% extends 'base.html.twig' %}
{% block title %}Saison {{id }}-program.title{% endblock %}

{% block body %}

       <h2> Saison {{ season.id }} de {{ program.title }}</h2>

    <p class="card-text">{{ season.description }}.</p>
    <div class="card border-dark mb-3"  style="max-width: 45rem;">
        <div class="card-header text-center" >Liste des épisodes</div>
        <div class="card text-white bg-dark mb-3">
            {% for episode in episodes %}
            <p>Episode{{ episode.id }}: {{ episode.title }}</p>
            {% endfor %}
        </div>
    </div>
    <div>
        <a href="{{ path('wild_program', {'slug':"walking-dead"}) }}">
            <button type="button" class="btn btn-danger"> Retour</button>
        </a>
    </div>
{% endblock %}
